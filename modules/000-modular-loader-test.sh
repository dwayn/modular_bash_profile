# This is just a simple module to test that all the sourcing setup works correctly. It can be symlinked
# in the enabled folder and then source modular_profile_loader.sh from the terminal to see if this
# outputs correctly with no errors.

echo "Modular Bash profile loader is working correctly!"